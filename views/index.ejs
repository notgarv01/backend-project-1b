<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10">
            <form action="/create" method="post">
                <input type="text" placeholder="Title goes here" class="bg-zinc-800 w-full block px-5 py-2 rounded-md outline-none" name="title">
                <textarea class="mt-2 resize-none bg-zinc-800 border-zinc-100 block px-5 py-2 w-full rounded-md outline-none" placeholder="Write your tasks details here" name="details"></textarea>
                <input type="submit" value="Create Task" class="bg-blue-900 rounded-lg p-2 mt-3 text-lg">
            </form>
        </div>
        <div class="tasks p-10 flex gap-3 flex-wrap">
            <% if(files.length > 0){ %>
                <% files.forEach(function(val){%>
                    <div class="task px-3 py-2 bg-zinc-800 w-72 h-20 rounded-md text-2xl tracking-tight">
                        <h1><%= val %></h1>
                        <a class="text-blue-500 inline-block mt-1" href="/file/<%= val%>">Read More...</a>
                        <!-- <p>Lorem ipsum dolor sit amet consectetur adipisicing elit. Exercitationem, voluptates!</p> -->
                    </div>
              <%  })%>
           <% } else{ %>
            <h1>No tasks yet!!</h1>
        <%} %>
        </div>
    </div>
</body>
<script src="/public/javascripts/script.js"></script>
</html>